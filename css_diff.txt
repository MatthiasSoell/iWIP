diff --git a/static/css/custom_reveal.css b/static/css/custom_reveal.css
index 4558171..1580a4b 100644
--- a/static/css/custom_reveal.css
+++ b/static/css/custom_reveal.css
@@ -310,17 +310,34 @@
   .reveal section .figure-frame.is-sm,
   .reveal section .figure-frame.is-ssm,
   .reveal section .figure-frame.is-lg {
-    width: auto;
-    max-width: 96vw;
+    --figure-landscape-max-width: min(88vw, 1100px);
+    --figure-landscape-max-height: min(68vh, calc(100vh - 7rem));
+    width: min(100%, var(--figure-landscape-max-width));
+    max-width: var(--figure-landscape-max-width);
     margin: 0 auto 0.35rem;
   }
 
+  @supports (height: 100dvh) {
+    .reveal section .figure-frame,
+    .reveal section .figure-frame.figure-frame-sm,
+    .reveal section .figure-frame.figure-frame-ssm,
+    .reveal section .figure-frame.figure-frame-lg,
+    .reveal section .figure-frame.is-sm,
+    .reveal section .figure-frame.is-ssm,
+    .reveal section .figure-frame.is-lg {
+      --figure-landscape-max-height: min(68dvh, calc(100dvh - 7rem));
+    }
+  }
+
   .reveal section .figure-frame > img,
+  .reveal section .figure-frame > .iwip_reveal_image_trigger img,
   .reveal section img.figure-frame {
-    width: auto;
+    width: 100%;
     max-width: 100%;
     height: auto;
-    max-height: 64vh;
+    max-height: var(--figure-landscape-max-height);
+    object-fit: contain;
+    margin: 0 auto;
   }
 
   .reveal .bild-quelle {
@@ -1642,12 +1659,27 @@ body.presentation-view footer {
   position: fixed;
   inset: 0;
   z-index: 1200;
-  display: grid;
-  place-items: center;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  min-height: 100vh;
   padding: 1rem;
+  box-sizing: border-box;
+  overflow: auto;
   background: rgba(0, 0, 0, 0.45);
 }
 
+@supports (min-height: 100dvh) {
+  .iwip_overlay {
+    min-height: 100dvh;
+  }
+
+  .iwip_overlay--image {
+    --iwip-overlay-image-vw: 95dvw;
+    --iwip-overlay-image-vh: 95dvh;
+  }
+}
+
 .iwip_overlay_panel {
   position: relative;
   background: #fff;
@@ -1683,14 +1715,22 @@ body.presentation-view footer {
 }
 
 .iwip_overlay--image .iwip_overlay_panel {
-  width: min(92vw, 1200px);
-  max-height: 90vh;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  width: auto;
+  max-width: min(var(--iwip-overlay-image-vw), 1200px);
+  max-height: var(--iwip-overlay-image-vh);
   padding: 0.9rem;
+  box-sizing: border-box;
 }
 
 .iwip_overlay--image {
+  --iwip-overlay-image-vw: 95vw;
+  --iwip-overlay-image-vh: 95vh;
   z-index: 3200;
   background: rgba(255, 255, 255, 0.92);
+  overflow: hidden;
   cursor: zoom-out;
 }
 
@@ -1700,14 +1740,17 @@ body.presentation-view footer {
 }
 
 .iwip_overlay--image .iwip_reveal_image_wrap {
-  display: grid;
-  place-items: center;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  max-width: calc(var(--iwip-overlay-image-vw) - 1.8rem);
+  max-height: calc(var(--iwip-overlay-image-vh) - 1.8rem);
 }
 
 .iwip_overlay--image .iwip_reveal_image_wrap img {
   display: block;
-  max-width: 95vw;
-  max-height: 95vh;
+  max-width: min(100%, calc(var(--iwip-overlay-image-vw) - 1.8rem));
+  max-height: min(100%, calc(var(--iwip-overlay-image-vh) - 1.8rem));
   width: auto;
   height: auto;
   margin: 0 auto;
@@ -1715,6 +1758,67 @@ body.presentation-view footer {
   cursor: zoom-out;
 }
 
+@media (max-width: 767px) {
+  .iwip_overlay--image {
+    --iwip-overlay-mobile-gap: 0.35rem;
+    --iwip-overlay-image-vw: calc(100vw - (2 * var(--iwip-overlay-mobile-gap)));
+    --iwip-overlay-image-vh: calc(100vh - (2 * var(--iwip-overlay-mobile-gap)));
+    padding: var(--iwip-overlay-mobile-gap);
+  }
+
+  @supports (min-height: 100dvh) {
+    .iwip_overlay--image {
+      --iwip-overlay-image-vw: calc(100dvw - (2 * var(--iwip-overlay-mobile-gap)));
+      --iwip-overlay-image-vh: calc(100dvh - (2 * var(--iwip-overlay-mobile-gap)));
+    }
+  }
+
+  .iwip_overlay--image .iwip_overlay_panel {
+    padding: 0.35rem;
+  }
+
+  .iwip_overlay--image .iwip_reveal_image_wrap {
+    max-width: calc(var(--iwip-overlay-image-vw) - 0.7rem);
+    max-height: calc(var(--iwip-overlay-image-vh) - 0.7rem);
+  }
+
+  .iwip_overlay--image .iwip_reveal_image_wrap img {
+    max-width: min(100%, calc(var(--iwip-overlay-image-vw) - 0.7rem));
+    max-height: min(100%, calc(var(--iwip-overlay-image-vh) - 0.7rem));
+  }
+}
+
+@media screen and (max-height: 500px) and (orientation: landscape) {
+  .iwip_overlay--image {
+    --iwip-overlay-mobile-gap: 0.35rem;
+    --iwip-overlay-image-vw: 94vw;
+    --iwip-overlay-image-vh: 78vh;
+    padding: var(--iwip-overlay-mobile-gap);
+  }
+
+  @supports (height: 100dvh) {
+    .iwip_overlay--image {
+      --iwip-overlay-image-vh: 78dvh;
+    }
+  }
+
+  .iwip_overlay--image .iwip_overlay_panel {
+    max-width: min(var(--iwip-overlay-image-vw), 1200px);
+    max-height: var(--iwip-overlay-image-vh);
+    padding: 0.35rem;
+  }
+
+  .iwip_overlay--image .iwip_reveal_image_wrap {
+    max-width: calc(var(--iwip-overlay-image-vw) - 0.7rem);
+    max-height: calc(var(--iwip-overlay-image-vh) - 0.7rem);
+  }
+
+  .iwip_overlay--image .iwip_reveal_image_wrap img {
+    max-width: min(100%, calc(var(--iwip-overlay-image-vw) - 0.7rem));
+    max-height: min(100%, calc(var(--iwip-overlay-image-vh) - 0.7rem));
+  }
+}
+
 .iwip_overlay--qr .iwip_qr_wrap {
   width: 100%;
   height: 100%;
